import { GetSensorNamePipe } from './get-sensor-name.pipe';

describe('GetSensorNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetSensorNamePipe();
    expect(pipe).toBeTruthy();
  });
});
